server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Required for client-side routing (React Router)
    location / {
        try_files $uri /index.html;
    }

    # Static files
    location /assets {
        try_files $uri =404;
    }
}

